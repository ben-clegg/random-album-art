extends layout

block main
  div.result
    h2 Artist: 
      i= `${albumArtist}`
    
    h2 Title 
    p.albumtitle= `${albumTitle}`
    p.info (Use any part of this as your album's title.) 
    
    h2 Base Artwork
    a(href=imgLink)
      img(src=imgSrc)
      p.info (Click to download.)
